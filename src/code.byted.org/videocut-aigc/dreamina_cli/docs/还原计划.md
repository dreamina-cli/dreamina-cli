# 还原与整理说明

本文不是待办清单，而是本仓库已执行的整理方向说明。

## 已完成的方向

### 1. 保留原始路径层级

Git 根目录放在 `/opt/tiger`，但项目仍保留：

```text
src/code.byted.org/videocut-aigc/dreamina_cli
```

这样既能初始化独立仓库，也不会破坏源码与文档里对原路径层级的引用。

### 2. 文档归档

- 项目根仅保留项目 README
- 其余分析文档集中到 `docs/`
- Markdown 统一使用中文

### 3. 运行产物与示例资源分离

- `dist/` 用于构建产物
- `logs/`、`tmp_smoke_*` 视为运行临时文件
- `testdata/smoke/` 保留稳定示例资源

### 4. 工程化补齐

- 增加仓库根 `README.md`
- 增加仓库根 `AGENTS.md`
- 增加跨平台构建脚本
- 以 `/opt/tiger` 为 Git 根准备初始化和推送

## 当前结论

仓库已经从“恢复资料散落在项目根目录”的状态，切换为“可继续维护的工程目录”。
